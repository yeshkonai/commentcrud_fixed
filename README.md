# Task Comments REST API (CRM Module)

REST API модуль **«Комментарии к задачам»** для CRM-системы.

Проект реализован в рамках тестового задания с использованием **NestJS**, **TypeORM**, **PostgreSQL** и **Docker**.

---

## 🛠️ Технологии

* Node.js
* NestJS
* TypeORM
* PostgreSQL
* TypeScript
* Docker & Docker Compose
* Swagger (OpenAPI)

---

## 📌 Функциональность

### Сущность Comment

| Поле       | Тип       | Описание                            |
| ---------- | --------- | ----------------------------------- |
| id         | UUID      | Уникальный идентификатор            |
| task_id    | UUID      | ID задачи                           |
| author_id  | UUID      | ID автора комментария               |
| text       | string    | Текст комментария (1–1000 символов) |
| created_at | timestamp | Дата создания                       |
| updated_at | timestamp | Дата обновления                     |

---

## 🔗 Эндпоинты

| Метод  | URL                      | Описание                       |
| ------ | ------------------------ | ------------------------------ |
| POST   | `/comments`              | Создать комментарий            |
| GET    | `/comments?task_id=UUID` | Получить комментарии по задаче |
| GET    | `/comments/:id`          | Получить комментарий по ID     |
| PATCH  | `/comments/:id`          | Редактировать комментарий      |
| DELETE | `/comments/:id`          | Удалить комментарий            |

---

## ⚖️ Бизнес-правила

1. Редактировать и удалять комментарий может **только автор**
2. Текст комментария обязателен (от 1 до 1000 символов)
3. Комментарии возвращаются **отсортированными по дате** (новые первыми)

Для проверки авторства используется заголовок:

```
x-author-id: <UUID автора>
```

---

## 🚀 Запуск проекта (Docker)

### 1️⃣ Клонировать репозиторий

```bash
git clone https://github.com/<your-username>/<repo-name>.git
cd <repo-name>
```

### 2️⃣ Запустить контейнеры

```bash
docker compose up -d --build
```

---

## 🌐 Swagger документация

После запуска доступна по адресу:

```
http://localhost:3000/api
```

---

## 📄 Примеры запросов

### Создание комментария

```http
POST /comments
Content-Type: application/json

{
  "task_id": "123e4567-e89b-12d3-a456-426614174000",
  "author_id": "123e4567-e89b-12d3-a456-426614174001",
  "text": "Это комментарий к задаче"
}
```

### Редактирование комментария

```http
PATCH /comments/{id}
x-author-id: 123e4567-e89b-12d3-a456-426614174001
```

### Удаление комментария

```http
DELETE /comments/{id}
x-author-id: 123e4567-e89b-12d3-a456-426614174001
```

---

## 📦 Docker

В проекте используется:

* PostgreSQL в Docker контейнере
* NestJS приложение в отдельном контейнере

---

## ✅ Статус

Проект полностью соответствует требованиям тестового задания и готов к проверке.
